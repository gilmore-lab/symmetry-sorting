fill=Group,
geom="boxplot")
qplot(data=jaccard.df,
x=as.factor(Exemplar.1), y=Jaccard,
facets = Group ~ .,
fill=Group,
geom="boxplot")
with( jaccard.df, max(Exemplar.1))
View(jaccard.df)
jaccard.all.df <- read.csv("jaccard-all.csv")
jaccard.select <- with(jaccard.all.df, !(Exemplar.Row==Exemplar.Col))
qplot(data=jaccard.df[jaccard.select,],
x=as.factor(Exemplar.Row), y=Jaccard,
facets = Group ~ .,
fill=Group,
geom="boxplot")
qplot(data=jaccard.all.df[jaccard.select,],
x=as.factor(Exemplar.Row), y=Jaccard,
facets = Group ~ .,
fill=Group,
geom="boxplot")
ls()
class(ls())
l <- ls()
l[1]
names(jaccard.all.df)
help(rename)
n.sets.df <- read.csv("number-of-sets.csv")
with( n.sets.df, Partcipant <- Participant.. )
n.sets.df
jaccard.trim.df <- jaccard.all.df[jaccard.select,]
jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard)) %>%
arrange(Jaccard.mean)
require(ggplot2)
require(dplyr)
jaccard.df <- read.csv("jaccard.csv")
str(jaccard.df)
theme.symm <- theme( axis.title.x = element_text(size=18),
axis.title.y = element_text(size=18),
strip.text = element_text(size=16),
axis.text = element_text(size=14),
legend.text = element_text(size=14),
legend.title = element_text(size=14)
)
qplot(x=Jaccard,
data=jaccard.df,
geom=c("histogram"),
facets = Group ~ ., fill=Group ) +
labs( x="Jaccard Index", y="N observations") +
theme.symm +
guides( fill=FALSE )
jaccard.all.df <- read.csv("jaccard-all.csv")
# this is similar to jaccard.csv, but includes a,b and b,a scores for each
# exemplar pair. Of course j(a,b)=j(b,a), but we need both scores to show
# exemplar-by-exemplar patterns.
# Select a,b and b,a but not a,a pairs
jaccard.select <- with(jaccard.all.df, !(Exemplar.Row==Exemplar.Col))
qplot(data=jaccard.all.df[jaccard.select,],
x=as.factor(Exemplar.Row), y=Jaccard,
facets = Group ~ .,
fill=Group,
geom="boxplot")
jaccard.trim.df <- jaccard.all.df[jaccard.select,]
jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard),
Jaccard.max.mean = max(Jaccard.mean),
Jaccard.min.sem = min(Jaccard.sem))
jaccard.trim.df <- jaccard.all.df[jaccard.select,]
jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard),
Jaccard.max.mean = max(Jaccard.mean),
Jaccard.min.sem = min(Jaccard.sem))
jaccard.trim.best <- jaccard.trim.df %>%
filter(Group, Exemplar, Jaccard.mean == Jaccard.max.mean )
jaccard.trim.best <- jaccard.trim.df %>%
filter(Group, Exemplar.Row, Jaccard.mean == Jaccard.max.mean )
jaccard.trim.df
names(jaccard.trim.df)
jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard),
Jaccard.max.mean = max(Jaccard.mean),
Jaccard.min.sem = min(Jaccard.sem))
names(jaccard.trim.df)
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard),
Jaccard.max.mean = max(Jaccard.mean),
Jaccard.min.sem = min(Jaccard.sem))
jaccard.trim.best <- jaccard.trim.summ %>%
filter(Group, Exemplar.Row, Jaccard.mean == Jaccard.max.mean )
View(jaccard.trim.summ)
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard)) %>%
summarize( Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.trim.summ
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard)) %>%
summarize( Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.trim.summ
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group, Exemplar.Row) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem)) %>%
filter(Group, Exemplar.Row, Jaccard.mean == Jaccard.max.mean )
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group, Exemplar.Row) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard))
jaccard.trim.summ
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group, Exemplar.Row) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.trim.best
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group ) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.trim.best
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group ) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.max.med=max(Jaccard.med),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.med=min(Jaccard.med)
Jaccard.min.sem=min(Jaccard.sem)) %>%
filter(Group, Exemplar.Row, Jaccard.mean == Jaccard.max.mean )
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x=Group, by.y=Group)
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x="Group"", by.y="Group")
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x="Group", by.y="Group")
jaccard.stats
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group ) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.max.med=max(Jaccard.med),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.med=min(Jaccard.med),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x="Group", by.y="Group")
jaccard.stats
jaccard.stats %>%
filter( Group, Exemplar.Row, Jaccard.mean==Jaccard.max.mean )
jaccard.stats
jaccard.stats %>%
filter( Group, Exemplar.Row, Jaccard.mean>=Jaccard.max.mean )
s <- jaccard.stats[ jaccard.stats$Jaccard.mean == jaccard.stats$Jaccard.max.mean, ]
s
jaccard.stats[ jaccard.stats$Jaccard.med == jaccard.stats$Jaccard.max.med, ]
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard))
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group ) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.max.med=max(Jaccard.med),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.med=min(Jaccard.med),
Jaccard.min.sem=min(Jaccard.sem))
View(jaccard.trim.best)
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x="Group", by.y="Group")
View(jaccard.stats)
jaccard.stats %>%
filter( Jaccard.mean>=Jaccard.max.mean )
jaccard.stats %>%
filter( Jaccard.mean>=Jaccard.max.mean )
jaccard.status %>%
filter( Jaccard.med>=Jaccard.max.med )
jaccard.stats %>%
filter( Jaccard.min<=Jaccard.min.mean )
jaccard.status %>%
filter( Jaccard.sem>=Jaccard.min.sem )
jaccard.stats %>%
filter( Jaccard.mean>=Jaccard.max.mean )
jaccard.stats %>%
filter( Jaccard.med>=Jaccard.max.med )
jaccard.stats %>%
filter( Jaccard.min<=Jaccard.min.mean )
jaccard.stats %>%
filter( Jaccard.sem>=Jaccard.min.sem )
jaccard.stats %>%
filter( Jaccard.sem<=Jaccard.min.sem )
jaccard.stats %>%
filter( Jaccard.med>=Jaccard.max.med )
jaccard.stats %>%
filter( Jaccard.mean==Jaccard.max.mean )
jaccard.stats %>%
filter( Jaccard.med==Jaccard.max.med )
jaccard.stats
jaccard.stats %>%
filter( Jaccard.sem<=Jaccard.min.sem )
jaccard.stats <- jaccard.stats
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean ),
Min.mean = (Jaccard.mean==Jaccard.min.mean ),
Max.med = (Jaccard.med == Jaccard.max.med ),
Min.sem = (Jaccard.min == Jaccard.min.sem )
)
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean ),
Min.mean = (Jaccard.mean==Jaccard.min.mean ),
Max.med = (Jaccard.med == Jaccard.max.med ),
Min.sem = (Jaccard.min == Jaccard.min.sem )
)
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean ),
Min.mean = (Jaccard.mean==Jaccard.min.mean ),
Max.med = (Jaccard.med == Jaccard.max.med ),
Min.sem = (Jaccard.min == Jaccard.min.sem )
)
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean ))
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean),
Min.mean = (Jaccard.mean==Jaccard.min.mean),
Max.med = (Jaccard.med==Jaccard.max.med))
jaccard.stats
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean),
Min.mean = (Jaccard.mean==Jaccard.min.mean),
Max.med = (Jaccard.med==Jaccard.max.med),
Min.sem = (Jaccard.sem==Jaccard.min.sem)
)
jaccard.stats %>%
filter( Max.mean, Max.med )
jaccard.stats %>%
filter( Max.mean, Max.med ) %>%
select( Group, Exemplar.Row, Jaccard.mean )
jaccard.stats %>%
filter( Max.med, Min.sem ) %>%
select( Group, Exemplar.Row, Jaccard.mean )
# Max median and min sem
jaccard.stats %>%
filter( Max.med, Min.sem ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
# Max mean and min sem
jaccard.stats %>%
filter( Max.med, Min.sem ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
# Max mean only
jaccard.stats %>%
filter( Max.mean ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
jaccard.stats %>%
filter( Max.med ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med, Jaccard.sem ) %>%
sort_by( Jaccard.sem )
# Max med, but look at low sem
jaccard.stats %>%
filter( Max.med ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med, Jaccard.sem ) %>%
arrange( Group, Jaccard.sem )
require(lme4)
jaccard.lme <- lme( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar))
jaccard.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar))
jaccard.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar.Row))
summary(jaccard.lme)
jaccard.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar.Row:Group))
summary(jaccard.lme)
confint(jaccard.lme)
jaccard.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar.Row:Group))
summary(jaccard.lme)
confint(jaccard.lme)
jaccard.reduced.lme <- lmer( data=jaccard.trim.df, Jaccard ~ (1|Exemplar.Row:Group))
anova( jaccard.lme, jaccard.lme.reduced )
anova( jaccard.lme, jaccard.reduced.lme )
jaccard.norandom.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group)
n.sets.lme <- lmer( data=n.sets.df, NSets ~ Group + (1|ParticipantN))
summary(n.sets.lme)
n.sets.lme <- lmer( data=n.sets.df, Nsets ~ Group + (1|ParticipantN))
summary(n.sets.lme)
n.sets.reduced.lme <- update(n.sets.lme, drop(Group))
help(drop)
n.sets.reduced.lme <- update(n.sets.lme, lmer( data=n.sets.df, Nsets ~ (1|ParticipantN))
anova( n.sets.lme, n.sets.reduced.lme )
# drop Group
n.sets.reduced.lme <- lmer( data=n.sets.df, Nsets ~ (1|ParticipantN) )
anova( n.sets.lme, n.sets.reduced.lme )
anova(n.sets.lme)
anova( jaccard.lme )
jaccard.ci <- confint(jaccard.lme)
names( jaccard.ci)
jaccard.ci
dim(jaccard.ci)
jaccard.ci.df <- data.frame(jaccard.ci[2:,])
jaccard.ci.df <- data.frame(jaccard.ci[2:7,])
View(jaccard.ci.df)
View(jaccard.ci.df)
row.names(jaccard.ci)
jaccard.ci.df <- data.frame(jaccard.ci[2:7], Group=row.names(jaccard.ci[2:7,]))
jaccard.ci.df
jaccard.ci.df <- data.frame(jaccard.ci[2:7,], Group=row.names(jaccard.ci[2:7,]))
jaccard.ci.df
jaccard.ci[2:7,]
jaccard.ci[,]
jaccard.ci[3:7,]
jaccard.ci.df <- data.frame(jaccard.ci[3:7,], Group=row.names(jaccard.ci[3:7,]))
jaccard.ci.df
jaccard.ci.df <- data.frame(jaccard.ci[3:7,], Group=row.names(jaccard.ci[3:7,]), row.names=FALSE)
jaccard.ci.df <- data.frame(jaccard.ci[3:7,], Group=row.names(jaccard.ci[3:7,]))
with(jaccard.ci.df, Group[1,]<-"GroupP1")
with(jaccard.ci.df, Group[1]<-"GroupP1")
jaccard.ci.df$Group[1] <- "GroupP1"
name( jaccard.ci.df$Group[1]) <- "GroupP1"
levels(jaccard.ci.df$Group[1]) <- "GroupP1"
levels( jaccard.ci.df$Group)
levels( jaccard.ci.df$Group)[1] <- "GroupP1"
jaccard.ci.df$Group
help(data.frame)
jaccard.ci.df <- data.frame(jaccard.ci[3:7], Group=row.names(jaccard.ci[3:7,]), row.names=NULL)
jaccard.ci.df
jaccard.ci.df <- data.frame(jaccard.ci[3:7,], Group=row.names(jaccard.ci[3:7,]), row.names=NULL)
jaccard.ci.df$Group
levels( jaccard.ci.df$Group)[1] <- "GroupP1"
names( jaccard.ci.df )
names( jaccard.ci.df ) <- c("Low", "High", "Group")
qplot( data=jaccard.ci.df, x=Group, y=Low.Val)
names( jaccard.ci.df ) <- c("Low.Val", "High.Val", "Group")
qplot( data=jaccard.ci.df, x=Group, y=Low.Val)
# rows 1 and 2 have values we don't care about
Jacc.vals <- rbind( jaccard.ci[3:7,1], jaccard.ci[3:7,2] )
Group <- rep( c("P1", "P31M", "P3M1", "P6", "P6M"), 2)
LowHigh <- rep(c("Low","High"), each=5)
jaccard.ci.df <- data.frame(Group, Jacc.vals, LowHigh, row.names=NULL)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.vals, color=LowHigh)
jaccard.ci.df <- data.frame(Group, Jacc.vals, LowHigh, row.names=NULL)
View(jaccard.ci.df)
Jacc.vals <- rbind( jaccard.ci[3:7,1], jaccard.ci[3:7,2] )
Jacc.vals
Jacc.vals <- cbind( jaccard.ci[3:7,1], jaccard.ci[3:7,2] )
Jacc.vals
Jacc.vals <- c( jaccard.ci[3:7,1], jaccard.ci[3:7,2] )
names(Jacc.vals)
Group <- rep( c("P1", "P31M", "P3M1", "P6", "P6M"), 2)
LowHigh <- rep(c("Low","High"), each=5)
jaccard.ci.df <- data.frame(Group, Jacc.vals, LowHigh, row.names=NULL)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.vals, color=LowHigh)
J.offset <-c(0, rep( jaccard.ci[3,1],4), 0, rep(jaccard.ci[3,2],4) )
J.offset <-c(0, rep( jaccard.ci[3,1],4), 0, rep(jaccard.ci[3,2],4) )
Jacc.fitted <- Jacc.vals + J.offset
Group <- rep( c("P1", "P31M", "P3M1", "P6", "P6M"), 2)
LowHigh <- rep(c("Low","High"), each=5)
jaccard.ci.df <- data.frame(Group, Jacc.fitted, LowHigh, row.names=NULL)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.vals, color=LowHigh)
Jacc.fitted
jaccard.ci.df <- data.frame(Group, Jacc.fitted, LowHigh, row.names=NULL)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=LowHigh)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=LowHigh) +
labs(x="Group", y="CI for Mean Jaccard")
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=LowHigh) +
labs(x="Group", y="CI for Mean Jaccard") + theme.symm
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=LowHigh) +
labs(x="Group", y="CI for Mean Jaccard") + theme.symm + guides(color=FALSE)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=LowHigh, geom="line", group=Group) +
labs(x="Group", y="CI for Mean Jaccard") + theme.symm + guides(color=FALSE)
qplot( data=jaccard.ci.df, x=Group, y=Jacc.fitted, color=Group, geom="line", group=Group) +
labs(x="Group", y="CI for Mean Jaccard") + theme.symm + guides(color=FALSE)
## Set-up
require(ggplot2)
require(dplyr)
## Jaccard indices across exemplars, participants
jaccard.df <- read.csv("jaccard.csv")
str(jaccard.df)
### Histogram
theme.symm <- theme( axis.title.x = element_text(size=18),
axis.title.y = element_text(size=18),
strip.text = element_text(size=16),
axis.text = element_text(size=14),
legend.text = element_text(size=14),
legend.title = element_text(size=14)
)
qplot(x=Jaccard,
data=jaccard.df,
geom=c("histogram"),
facets = Group ~ ., fill=Group ) +
labs( x="Jaccard Index", y="N observations") +
theme.symm +
guides( fill=FALSE )
## Jaccard indices by exemplar
jaccard.all.df <- read.csv("jaccard-all.csv")
# this is similar to jaccard.csv, but includes a,b and b,a scores for each
# exemplar pair. Of course j(a,b)=j(b,a), but we need both scores to show
# exemplar-by-exemplar patterns.
# Select a,b and b,a but not a,a pairs
jaccard.select <- with(jaccard.all.df, !(Exemplar.Row==Exemplar.Col))
```
### Boxplot
qplot(data=jaccard.all.df[jaccard.select,],
x=as.factor(Exemplar.Row), y=Jaccard,
facets = Group ~ .,
fill=Group,
geom="boxplot")
### Means, Medians, SE by exemplar
jaccard.trim.df <- jaccard.all.df[jaccard.select,]
jaccard.trim.summ <- jaccard.trim.df %>%
group_by(Group, Exemplar.Row) %>%
summarize( Jaccard.mean=mean(Jaccard),
Jaccard.sem=mean(Jaccard)/sqrt(n()),
Jaccard.med=median(Jaccard))
jaccard.trim.best <- jaccard.trim.summ %>%
group_by( Group ) %>%
summarize(Jaccard.max.mean=max(Jaccard.mean),
Jaccard.max.med=max(Jaccard.med),
Jaccard.min.mean=min(Jaccard.mean),
Jaccard.min.med=min(Jaccard.med),
Jaccard.min.sem=min(Jaccard.sem))
jaccard.stats <- merge(jaccard.trim.summ, jaccard.trim.best, by.x="Group", by.y="Group")
jaccard.stats <- jaccard.stats %>%
mutate( Max.mean = (Jaccard.mean==Jaccard.max.mean),
Min.mean = (Jaccard.mean==Jaccard.min.mean),
Max.med = (Jaccard.med==Jaccard.max.med),
Min.sem = (Jaccard.sem==Jaccard.min.sem)
)
#### Max mean and max median
jaccard.stats %>%
filter( Max.mean, Max.med ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
#### Max median and min sem
jaccard.stats %>%
filter( Max.med, Min.sem ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
#### Max mean and min sem
jaccard.stats %>%
filter( Max.med, Min.sem ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
#### Max mean only
jaccard.stats %>%
filter( Max.mean ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med )
#### Max med, but look at low sem
jaccard.stats %>%
filter( Max.med ) %>%
select( Group, Exemplar.Row, Jaccard.mean, Jaccard.med, Jaccard.sem ) %>%
arrange( Group, Jaccard.sem )
### Linear mixed effects model on Jaccard
Fixed effect of group, random intercept for Group:Exemplar combination.
require(lme4)
jaccard.lme <- lmer( data=jaccard.trim.df, Jaccard ~ Group + (1|Exemplar.Row:Group))
summary(jaccard.lme)
jaccard.ci <- confint(jaccard.lme)
jaccard.reduced.lme <- lmer( data=jaccard.trim.df, Jaccard ~ (1|Exemplar.Row:Group))
anova( jaccard.lme, jaccard.reduced.lme )
# Full model has better fit $\chi^{2}(df=4)=93.431, p<2.2e-16$
#### Plot of effects CI
# rows 1 and 2 have values we don't care about
Jacc.vals <- c( jaccard.ci[3:7,1], jaccard.ci[3:7,2] )
J.offset <-c(0, rep( jaccard.ci[3,1],4), 0, rep(jaccard.ci[3,2],4) )
Jacc.fitted <- Jacc.vals + J.offset
Group <- rep( c("P1", "P31M", "P3M1", "P6", "P6M"), 2)
LowHigh <- rep(c("Low","High"), each=5)
jaccard.ci.df <- data.frame(Group, Jacc.fitted, LowHigh, row.names=NULL)
qplot( data=jaccard.ci.df,
x=Group,
y=Jacc.fitted,
color=Group,
geom="line",
group=Group) +
labs(x="Group",
y="95% CI for Mean Jaccard") +
theme.symm + guides(color=FALSE)
P6<P6M, P3M1<P1, P1=P31M=P6M
## Number of sets
n.sets.df <- read.csv("number-of-sets.csv")
str(n.sets.df)
### Dotplot
qplot(data=n.sets.df, x=Nsets, geom=c("dotplot"), facets=Group ~ ., dotsize=.7, fill=Group ) +
labs( x="Number of Sets", y="") +
theme.symm +
theme( axis.text.y = element_blank() ) +
scale_y_continuous(breaks=NULL) +
guides( fill=FALSE )
### Linear mixed-effects model
Fixed effect of Group, random effect for Participant.
n.sets.lme <- lmer( data=n.sets.df, Nsets ~ Group + (1|ParticipantN))
summary(n.sets.lme)
# drop Group
n.sets.reduced.lme <- lmer( data=n.sets.df, Nsets ~ (1|ParticipantN) )
anova( n.sets.lme, n.sets.reduced.lme )
# Full model better fitting $\chi^{2}(df=4)=9.7039, p=.04572$. So, there are differences in the number of sets. P1 smallest, P6 largest.
